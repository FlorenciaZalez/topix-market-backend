# Topix Market Frontend

## Run locally

1. Install dependencies with `npm install`.
2. Copy `.env.example` to `.env`.
3. Start the dev server with `npm run dev`.

The frontend expects the FastAPI backend at `VITE_API_URL`.